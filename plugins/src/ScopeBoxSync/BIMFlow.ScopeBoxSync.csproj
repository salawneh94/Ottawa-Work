<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BIMFlow.ScopeBoxSync</RootNamespace>
    <AssemblyName>BIMFlow.ScopeBoxSync</AssemblyName>
    <!-- WPF for the dark-themed dialog (ScopeBoxSyncWindow); see BIMFlow.Shared's csproj comment. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shared\BIMFlow.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="ScopeBoxSync.addin">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
