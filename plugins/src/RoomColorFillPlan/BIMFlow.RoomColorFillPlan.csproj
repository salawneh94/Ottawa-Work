<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BIMFlow.RoomColorFillPlan</RootNamespace>
    <AssemblyName>BIMFlow.RoomColorFillPlan</AssemblyName>
    <!-- WPF for SimplePickerDialog, now in BIMFlow.Shared as WPF; see BIMFlow.Shared's csproj comment. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shared\BIMFlow.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="RoomColorFillPlan.addin">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
