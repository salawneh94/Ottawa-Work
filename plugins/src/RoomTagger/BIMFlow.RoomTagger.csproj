<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BIMFlow.RoomTagger</RootNamespace>
    <AssemblyName>BIMFlow.RoomTagger</AssemblyName>
    <!-- WPF for the dark-themed dialog (RoomTaggerWindow), same reasoning as RoomInventory's csproj. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shared\BIMFlow.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="RoomTagger.addin">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
