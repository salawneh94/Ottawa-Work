<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>OttawaWork.UnplacedCleaner</RootNamespace>
    <AssemblyName>OttawaWork.UnplacedCleaner</AssemblyName>
    <!-- WPF for the dark-themed dialog (UnplacedCleanerWindow); see OttawaWork.Shared's csproj comment. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shared\OttawaWork.Shared.csproj" />
  </ItemGroup>

</Project>
