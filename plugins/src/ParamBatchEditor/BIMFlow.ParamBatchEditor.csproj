<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BIMFlow.ParamBatchEditor</RootNamespace>
    <AssemblyName>BIMFlow.ParamBatchEditor</AssemblyName>
    <!-- WPF for the dark-themed dialog (ParamBatchEditorWindow) — see BIMFlow.Shared's csproj comment. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shared\BIMFlow.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="ParamBatchEditor.addin">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
