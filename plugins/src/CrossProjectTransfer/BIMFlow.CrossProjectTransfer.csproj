<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BIMFlow.CrossProjectTransfer</RootNamespace>
    <AssemblyName>BIMFlow.CrossProjectTransfer</AssemblyName>
    <!-- WPF for SimplePickerDialog, now in BIMFlow.Shared as WPF; see BIMFlow.Shared's csproj comment. -->
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shared\BIMFlow.Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="CrossProjectTransfer.addin">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

</Project>
